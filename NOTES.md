# 작업 시 알아둘 것

이 저장소에서 작업하거나 배포하기 전에 읽어야 할 내용을 모았습니다.
코드만 봐서는 알기 어렵거나, 모르면 사고가 나는 것들입니다.

---

## 1. 배포 전 반드시 해결해야 하는 것

### 1-1. 지금 로그인은 보안 기능이 아닙니다

- **아이디와 비밀번호가 비어 있지만 않으면 전부 통과합니다.** 계정 확인을 하지 않습니다.
- 검사를 브라우저에서만 합니다. 개발자 도구로 `localStorage` 의
  `nocode-auth` 값을 직접 넣으면 로그인 화면을 건너뜁니다.
- 즉 **네트워크에 접근 가능한 사람은 누구나 모든 화면을 볼 수 있습니다.**

내부망이라고 안전한 것이 아닙니다. 감염된 사내 PC, 협력사 단말, 퇴사자 계정이
모두 "내부"입니다. 이 화면에는 서버 목록과 IP 가 들어 있습니다.

**고칠 위치**

- `lib/auth.tsx` 의 `requestLogin()` 안을 실제 API 호출로 바꾸기
- 보호가 필요한 화면은 `getServerSideProps` 나 미들웨어에서 **서버가** 검사하도록 하기
  (지금의 `components/auth/AuthGate.tsx` 는 화면 깜빡임을 막는 용도일 뿐입니다)

### 1-2. API 에 인증이 없습니다

`pages/api/servers/` 의 주소는 누구나 호출할 수 있습니다.

```
curl -X POST http://<주소>/api/servers -d '{...}'
```

로그인하지 않아도 서버를 등록하고 설정을 바꿀 수 있습니다.
1-1 의 인증을 붙일 때 **API 라우트에도 같이 적용해야 합니다.**

---

## 2. 배포 환경

### 2-1. 실행 중에는 외부 연결이 전혀 없습니다

- 코드에 외부 주소가 없습니다.
- Inter 폰트는 `next/font/google` 이 **빌드할 때 받아서** `.next/static/media/` 에
  넣습니다. 실행 중에 구글로 요청하지 않습니다.

→ 인터넷이 끊긴 내부망에서 정상 동작합니다.

### 2-2. 빌드하는 곳에는 인터넷이 필요합니다

폰트 다운로드와 `npm install` 때문입니다.
인터넷 되는 곳에서 빌드해 산출물을 옮기거나, 사내 npm 미러를 쓰면 됩니다.

### 2-3. Node 서버가 필요합니다

API 라우트가 있어서 **정적 내보내기(`output: export`)로는 동작하지 않습니다.**
`next build` 후 `next start` 로 띄워야 합니다.

---

## 3. 데이터가 어디에 저장되는가

| 데이터 | 저장 위치 | 다른 PC 에서 보이나 |
| --- | --- | --- |
| 서버 목록 | 서버의 `data/servers.json` | **보입니다** |
| 계정 목록 | 서버의 `data/accounts.json` | **보입니다** |
| 로그인 상태 | 브라우저 `localStorage` | 안 보임 |
| 화이트/블랙 모드 | 브라우저 `localStorage` | 안 보임 |
| 대시보드 상자 배치 | 브라우저 `localStorage` | 안 보임 |
| 저장된 아이디 | 브라우저 `localStorage` | 안 보임 |

### 3-1. `data/servers.json`

- `.gitignore` 에 들어 있습니다. 운영 데이터를 커밋하지 않기 위해서입니다.
- 파일이 없으면 `lib/servers/mockServers.ts` 의 `SEED_SERVERS` 로 처음 한 번 만듭니다.
  (계정은 `lib/accounts/seedAccounts.ts` 의 `SEED_ACCOUNTS`)
- **파일이 만들어진 뒤에는 `SEED_SERVERS` 를 고쳐도 반영되지 않습니다.**
  처음부터 다시 만들려면 `data/servers.json` 을 지우고 화면을 새로고침하세요.
- 백업이 없습니다. 이 파일이 사라지면 등록한 서버가 전부 사라집니다.

### 3-2. 동시 쓰기

여러 요청이 같은 파일을 동시에 고치면 내용이 날아갈 수 있어서,
`lib/servers/serverStore.ts` 에서 요청을 한 줄로 세워 하나씩 처리합니다.

**이 보호는 Node 프로세스가 하나일 때만 유효합니다.**
PM2 cluster 처럼 여러 개를 띄우면 깨집니다. DB 로 옮기기 전까지는 한 개로 띄우세요.

---

## 4. 백엔드를 붙일 때 고칠 지점

화면 코드는 그대로 두고 아래만 바꾸면 됩니다.

| 하려는 것 | 고칠 파일 |
| --- | --- |
| 파일 대신 DB 에 저장 | `lib/storage/jsonStore.ts` — 서버·계정 저장소가 공유합니다 |
| API 주소가 바뀌거나 토큰이 필요 | `lib/api/client.ts` 의 `API_BASE` / `buildHeaders` |
| 모든 API 에 인증 검사 | `lib/api/handler.ts` 의 `methodHandler` |
| 로그인을 실제 인증으로 | `lib/auth.tsx` 의 `requestLogin()` |
| 검색·쪽 나누기를 서버에서 | `lib/useTableState.ts` 에 넘기는 `applyFilters` 를 서버 호출로 |
| 실제 핑 보내기 | `lib/servers/serverStore.ts` 의 `createServer()` (지금은 흉내) |

**같은 코드가 두 벌 있지 않도록 아래를 공유합니다.**
고칠 때 한쪽만 고치는 실수를 막기 위한 것이니, 새 화면을 만들 때도 가져다 쓰세요.

| 공유하는 것 | 쓰는 곳 |
| --- | --- |
| `lib/storage/jsonStore.ts` | 서버 저장소, 계정 저장소 |
| `lib/api/client.ts` | 브라우저에서 부르는 모든 API |
| `lib/api/handler.ts` | 모든 API 라우트 |
| `lib/useTableState.ts` | 서버관리, 계정관리 목록 |
| `components/ui/FilterForm.tsx` | 서버관리, 계정관리 검색 조건 |

### 4-1. 지금 핑은 가짜입니다

서버를 등록할 때 사용여부를 켜면 **핑을 쏜 척하고** 상태를 `up`,
응답시간을 10~50ms 사이 임의값으로 채웁니다. 실제로 아무 데도 접속하지 않습니다.

목록의 사용여부 토글도 마찬가지로 값만 바꿉니다.
실제 핑 발송/중단은 백엔드가 해야 합니다.

### 4-2. 검증 규칙은 두 곳에서 같이 씁니다

`lib/servers/validation.ts` 하나를 등록 화면과 API 가 함께 씁니다.
규칙을 고칠 때 한쪽만 고치지 마세요.

**API 쪽 검증은 지우면 안 됩니다.** 브라우저를 거치지 않은 요청이 들어올 수 있습니다.

### 4-3. IP 는 앞자리 0 을 막습니다

`01.1.1.1` 은 형식 오류로 거부합니다. `1.1.1.1` 과 같은 서버를 가리키는데
글자로는 달라서, 허용하면 **중복 IP 검사를 빠져나가 같은 서버가 두 번 등록됩니다.**
(중복 검사는 글자를 그대로 비교합니다)

나중에 IP 대신 도메인도 받게 된다면 같은 문제를 다시 검토해야 합니다.

### 4-4. IP 는 실제 호스트 주소만 받습니다

형식이 맞아도 서버 한 대를 가리키지 않는 주소는 전부 거부합니다.
(`lib/servers/validation.ts` 의 `getUnusableIpReason()`)

| 막는 주소 | 이유 |
| --- | --- |
| `0.x.x.x` | 목적지로 쓸 수 없는 대역 |
| `127.x.x.x` | 자기 자신(루프백) |
| `169.254.x.x` | DHCP 실패 시 붙는 임시 주소(링크 로컬) |
| `224~239.x.x.x` | 여러 대를 묶어 가리키는 그룹 주소(멀티캐스트) |
| `240~255.x.x.x` | 예약 대역. `255.255.255.255`(브로드캐스트) 포함 |
| 끝자리 `0` | 네트워크 주소 (`1.1.1.0`) |
| 끝자리 `255` | 브로드캐스트 (`1.1.1.255`) |

거부 사유는 주소마다 다른 문구로 보여 줍니다.

**끝자리 0 / 255 는 `/24` 대역을 전제로 막습니다.**
그보다 넓은 대역(예: `10.0.0.0/16`)에서는 `10.0.1.0` 이나 `10.0.1.255` 도
정상적인 서버 주소입니다. 그런 망을 감시하게 되면 `getUnusableIpReason()` 의
마지막 두 조건을 풀어야 합니다.

### 4-5. 계정 권한

권한 판단은 `lib/accounts/permissions.ts` 한 곳에 모았습니다.
화면에서 `role === "admin"` 을 직접 비교하지 마세요.

| 권한 | 할 수 있는 것 |
| --- | --- |
| `admin` (파트 관리자) | 소속 파트의 서버 등록, 소속 파트의 계정 관리 |
| `viewer` (일반) | 보기만 |

**관리자도 소속 파트만 다룹니다.** 전체를 보는 최고 관리자는 아직 없습니다.
필요해지면 `AccountRole` 에 역할을 하나 더 두고
`canAccessDivision()` 에서 예외를 주면 됩니다.

**아직 붙지 않았습니다.** 지금은 화면에서 권한으로 버튼을 숨기거나
목록을 거르지 않습니다. 로그인에 세션이 없어 "지금 누가 보고 있는지" 를
믿을 수 없기 때문입니다. 1-1 의 인증을 붙인 뒤에 적용해야 합니다.

화면에서 버튼을 숨기는 것은 편의일 뿐입니다. **실제 차단은 API 가 해야 합니다.**

### 4-6. 내 계정 화면은 로그인 아이디로 계정을 찾습니다

세션이 없어서, 목록을 받아 `loginId` 가 로그인한 아이디와 같은 줄을 찾아 씁니다.
(`lib/accounts/useAccounts.ts` 의 `useMyAccount`)

- **아이디만 알면 남의 정보도 볼 수 있습니다.** 백엔드가 붙으면
  `GET /api/accounts/me` 로 바꿔야 합니다.
- 개발 중에 화면을 확인하려면 씨앗 계정의 아이디로 로그인하세요.
  `admin.control` / `watch.control` / `admin.judicial` / `watch.registry` / `admin.family`
  (비밀번호는 아무 값이나 됩니다)

수정할 수 있는 것은 이름·이메일·번호뿐입니다.
아이디·소속 파트·권한을 요청 본문에 넣어도 API 가 무시합니다.

**비밀번호 변경은 형식 검사만 하고 실제로 바뀌지 않습니다.**
저장할 곳(비밀번호 해시)이 아직 없습니다.

### 4-7. 알림 소리를 붙일 때

내 계정의 "알림 받기" 는 계정에 저장합니다 (`Account.notifyEnabled`).
사람 단위 설정이라 다른 PC 로 로그인해도 따라오고, 나중에 메일·문자 같은
다른 통로가 생겨도 같은 값을 씁니다.

반대로 **화면 모드는 브라우저에 둡니다.** 기기마다 다를 수 있고,
화면이 그려지기 전에 적용돼야 해서 서버에서 받아올 수 없습니다.

**소리를 넣을 때 걸리는 것들**

1. **브라우저가 소리를 막습니다.** 사용자가 그 페이지에서 한 번이라도
   누르거나 입력하기 전에는 `audio.play()` 가 거부됩니다.
   대시보드를 띄워만 두면 소리가 안 납니다.
   → "알림 받기" 를 켜는 그 클릭에서 무음 오디오를 한 번 재생해 두고
     (이 동작으로 재생 권한이 열립니다) 그 객체를 재사용하는 방식이 흔합니다.

2. **끊김을 알려 줄 통로가 아직 없습니다.** 지금은 화면을 열 때 한 번 받아올 뿐,
   서버가 변화를 밀어 주지 않습니다. WebSocket 이나 주기적 조회가 필요합니다.

3. **같은 장애로 계속 울리면 안 됩니다.** 서버마다 "이미 알렸음" 을 기억해
   상태가 정상으로 돌아왔다가 다시 끊길 때만 울리도록 해야 합니다.

4. **탭을 여러 개 열어 두면 동시에 울립니다.** 한 탭만 울리게 하려면
   `BroadcastChannel` 등으로 조율이 필요합니다.

5. 소리를 끌 수 없는 상황(회의 중 등)을 위해 기기별 임시 음소거가
   따로 필요할 수 있습니다. 그것은 계정이 아니라 브라우저에 두는 값입니다.

## 5. 코드 작성 시 자주 걸리는 함정

### 5-1. 블랙 모드에서 안 보이는 색

`styles/globals.css` 의 `[data-theme="dark"]` 는 **일부 변수만 다시 정의합니다.**

- 다시 정의됨: `--primary-400`, `--primary-500`, `--primary-600`,
  텍스트 색, 배경/패널/구분선, 상태색
- **다시 정의 안 됨: `--primary-50/100/200/300/700/800/900`**

그래서 `text-primary-800` 처럼 밝은 모드 기준의 어두운 색을 쓰면
블랙 모드에서 어두운 배경에 묻혀 보이지 않습니다.

→ **글자와 아이콘에는 테마를 따라 바뀌는 토큰만 쓰세요.**
   `text-body`, `text-secondary`, `text-muted`, `primary-400~600`

실제로 로그인 화면에서 이 문제로 글자가 안 보인 적이 있습니다.

### 5-2. 외부 라이브러리 CSS 의 고정 색

`react-grid-layout` 의 크기 조절 손잡이가 `#0006` 으로 박혀 있어
블랙 모드에서 보이지 않았습니다. `styles/globals.css` 맨 아래에서 덮어썼습니다.

`_app.tsx` 는 **라이브러리 CSS 를 먼저, `globals.css` 를 나중에** 불러옵니다.
이 순서가 바뀌면 덮어쓰기가 먹지 않습니다.

### 5-3. SVG 아이콘은 `<img>` 로 쓰지 마세요

`public/icon/*.svg` 는 원본 보관용입니다. 화면에서는
`components/ui/icons.tsx` 의 컴포넌트를 씁니다.

`<img src="/icon/x.svg">` 로 불러오면 svg 안에 박힌 `#131927` 을 CSS 로 바꿀 수 없어
블랙 모드에서 아이콘이 안 보입니다. 컴포넌트 쪽은 `currentColor` 라 글자 색을 따라가고,
파일 요청도 생기지 않습니다.

아이콘을 추가할 때는 `public/icon/` 에 원본을 넣고 `icons.tsx` 에 같은 path 를
옮겨 적으면 됩니다. (두 곳을 같이 관리해야 합니다)

### 5-4. 브라우저 저장소는 화면이 뜬 뒤에 읽어야 합니다

서버에는 `localStorage` 가 없습니다. 첫 렌더에서 읽으면 서버가 그린 화면과
브라우저가 그린 화면이 달라져 경고가 나거나 화면이 한 번 튑니다.

이미 이 방식으로 처리된 것: 테마, 로그인 상태, 저장된 아이디, 대시보드 배치.
새로 추가할 때도 **항상 기본값으로 시작하고 `useEffect` 에서 읽으세요.**

### 5-5. 모달은 포털입니다

`components/ui/Modal.tsx` 는 `document.body` 에 그립니다.
사이드바에 `overflow-hidden` 이 걸려 있어서 그 안에 그리면 잘리기 때문입니다.

서버에는 `document` 가 없어서, 화면에 붙은 뒤에만 그리도록 막아 두었습니다.
이 장치를 빼면 처음부터 열린 모달이 있는 페이지가 **500 에러**가 납니다.

### 5-6. 업무구분은 id 로 저장하세요

`lib/businessDivisions.ts` 에서 화면에 보이는 이름(`label`)과
저장에 쓰는 값(`id`)을 나눠 두었습니다.

권한은 `["judicial", "registry"]` 처럼 **id 로** 저장됩니다.
한글 이름을 저장하면 나중에 "사법" → "사법업무" 로 바꿀 때 저장된 권한이 전부 깨집니다.

계정별 권한을 만들 때도 같은 `BUSINESS_DIVISIONS` 배열과
`canViewDivision()` 을 쓰세요.

---

## 6. 아직 동작하지 않는 것

### 6-1. 눌러도 아무 일 없는 버튼

서버관리 화면의 **선택삭제 / 엑셀 다운로드 / 일괄등록** 은 화면만 있습니다.

계정관리 화면에는 **계정 등록·수정 기능이 아직 없습니다.**
목록 보기, 검색, 사용여부 토글만 됩니다.

내 계정의 **비밀번호 변경**은 형식 검사만 합니다. (4-6 참고)

내 계정의 **알림 받기**는 값만 저장됩니다. 실제 알림과 소리는 아직 없습니다. (4-7 참고)

### 6-2. 브라우저마다 따로 노는 설정

대시보드 상자 배치와 화이트/블랙 모드는 브라우저에 저장됩니다.
**다른 PC 에서 로그인하면 기본값으로 보입니다.**
계정에 묶으려면 백엔드에 올려야 합니다.

사이드바 접힘 상태는 저장하지 않습니다. 새로고침하면 펼친 상태로 돌아옵니다.

### 6-3. 키보드로 대시보드 배치를 바꿀 수 없습니다

끌어서 옮기는 방식이라 키보드만으로는 불가능합니다.
"기본 배치로 되돌리기" 버튼이 유일한 대안입니다.

---

## 7. 참고

### 7-1. 지운 의존성

`lucide-react` 와 `@headlessui/react` 는 쓰는 곳이 없어 제거했습니다.
드롭다운·모달은 직접 만들어 쓰고 있습니다
(`components/ui/Modal.tsx`, `components/layout/sidebar/AccountMenu.tsx`).

### 7-2. 서버 등록 화면에서 받지 않는 값

표의 컬럼 중 **상태 / 응답(ms) / 마지막 확인** 은 등록 폼에 없습니다.
핑을 쏴 봐야 나오는 값이라 사람이 정할 수 있는 것이 아닙니다.

### 7-3. 등록 실패하면 입력하던 내용이 사라집니다

중복 IP 등으로 실패하면 등록 창이 닫히면서 초기화됩니다.
(바깥을 클릭해서 닫히지는 않지만, 실패 시에는 닫힙니다)

### 7-4. 저작권 표시 위치

두 곳에 있습니다.

- 로그인 화면 맨 아래 — `Copyright © 2026 ydh. All rights reserved.`
- 사이드바 맨 아래 — `© 2026 ydh` (11px, 흐린 색)

사이드바 쪽은 **모든 화면에 늘 떠 있지만 작업을 방해하지 않는 자리**로 골랐습니다.
본문에 넣으면 화면마다 관리해야 하고 내용과 섞이며, 로그인 화면에만 두면
평소에는 아무도 보지 않습니다. 사이드바를 접으면 자리가 없어 숨깁니다.
